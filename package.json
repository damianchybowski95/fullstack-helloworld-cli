{
  "name": "fullstack-helloworld-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "run_": "node ./dist/index.js",
    "install_": "npm install -g .",
    "start": "npm run build && npm run install_ && npx fullstack-cli"
  },
  "bin": {
    "fullstack-cli": "./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.3.2"
  }
}
